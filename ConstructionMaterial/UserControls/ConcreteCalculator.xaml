<UserControl
    x:Class="ConstructionMaterial.UserControls.ConcreteCalculator"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:ConstructionMaterial.UserControls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    d:DesignHeight="450"
    d:DesignWidth="350"
    mc:Ignorable="d">
    <Grid Margin="0,20,0,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="110" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <TextBlock
            Grid.Row="0"
            VerticalAlignment="Center"
            Style="{StaticResource FieldLabel}"
            Text="Element Type:" />
        <ComboBox
            x:Name="ElementTypeComboBox"
            Grid.Row="0"
            Grid.Column="1"
            Height="36"
            Margin="0,5"
            ItemsSource="{Binding ElementTypes}"
            SelectedItem="{Binding SelectedElementType, Mode=TwoWay}" />

        <TextBlock
            Grid.Row="1"
            VerticalAlignment="Center"
            Style="{StaticResource FieldLabel}"
            Text="Material Name:" />
        <ComboBox
            x:Name="MaterialComboBox"
            Grid.Row="1"
            Grid.Column="1"
            Height="36"
            Margin="0,5"
            DisplayMemberPath="Name"
            ItemsSource="{Binding MaterialNames}"
            SelectedItem="{Binding SelectedMaterial, Mode=TwoWay}" />

        <TextBlock
            Grid.Row="2"
            VerticalAlignment="Center"
            Style="{StaticResource FieldLabel}"
            Text="Length (m):" />
        <ui:TextBox
            x:Name="LengthTxt"
            Grid.Row="2"
            Grid.Column="1"
            Height="36"
            Margin="0,5"
            Text="{Binding Length, UpdateSourceTrigger=PropertyChanged}" />

        <TextBlock
            Grid.Row="3"
            VerticalAlignment="Center"
            Style="{StaticResource FieldLabel}"
            Text="Width (m):" />
        <ui:TextBox
            x:Name="WidthTxt"
            Grid.Row="3"
            Grid.Column="1"
            Height="36"
            Margin="0,5"
            Text="{Binding Width, UpdateSourceTrigger=PropertyChanged}" />

        <TextBlock
            Grid.Row="4"
            VerticalAlignment="Center"
            Style="{StaticResource FieldLabel}"
            Text="Depth (m):" />
        <ui:TextBox
            x:Name="DepthTxt"
            Grid.Row="4"
            Grid.Column="1"
            Height="36"
            Margin="0,5"
            Text="{Binding Depth, UpdateSourceTrigger=PropertyChanged}" />

        <TextBlock
            Grid.Row="5"
            VerticalAlignment="Center"
            Style="{StaticResource FieldLabel}"
            Text="Quantity:" />
        <ui:TextBox
            x:Name="QuantityTxt"
            Grid.Row="5"
            Grid.Column="1"
            Height="36"
            Margin="0,5"
            Text="{Binding Quantity, UpdateSourceTrigger=PropertyChanged}" />

        <StackPanel
            Grid.Row="6"
            Grid.Column="1"
            Margin="0,15"
            HorizontalAlignment="Right"
            Orientation="Horizontal">
            <ui:Button
                Name="ConcreteCalculateButton"
                Command="{Binding CalculateConcreteCommand}"
                Appearance="Primary"
                Height="36"
                Icon="{ui:SymbolIcon Symbol='Play24'}"
                Content="Calculate" />
            <ui:Button
                Name="SaveButton"
                Command="{Binding SaveConcreteCommand}"
                Appearance="Secondary"
                Height="36"
                Margin="10,0,0,0"
                Icon="{ui:SymbolIcon Symbol='Save24'}"
                Content="Create Order" />
        </StackPanel>

        <local:CalculationOutput
            x:Name="OutputControl"
            Grid.Row="7"
            Grid.ColumnSpan="2"
            OutputLabel="Total Order Value:"
            OutputValue="{Binding ResultText}" />
    </Grid>
</UserControl>
