<UserControl x:Class="ConstructionMaterial.UserControls.SteelCalculationTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ConstructionMaterial.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="250" 
             d:DesignWidth="400"
             Background="Transparent">

    <Grid Margin="0,20,0,0">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="110"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="Bar Diameter:"
               Grid.Row="0"
               Style="{StaticResource FieldLabel}"/>

        <ComboBox Grid.Row="0"
              Grid.Column="1"
              x:Name="BarDiameterComboBox"
              Height="32"
              Margin="0,5"
              ItemsSource="{Binding BarDiameters}"
              SelectedIndex="0"/>

        <TextBlock Text="Bar Length (m):"
               Grid.Row="1"
               Style="{StaticResource FieldLabel}"/>

        <TextBox Grid.Row="1"
             Grid.Column="1"
             TextChanged="TextBox_TextChanged"
             x:Name="BarLengthTxt"
             Style="{StaticResource ModernBox}"/>

        <TextBlock Text="Number Of Bars:"
               Grid.Row="2"
               Style="{StaticResource FieldLabel}"/>

        <TextBox Grid.Row="2"
             Grid.Column="1"
             TextChanged="TextBox_TextChanged"
             x:Name="NoOfBarsTxt"
             Style="{StaticResource ModernBox}"/>

        <StackPanel Grid.Row="3"
                Grid.ColumnSpan="2"
                Orientation="Horizontal"
                HorizontalAlignment="Right"
                Margin="0,15">

            <Button Content="Calculate"
                Style="{StaticResource ModernButton}"
                    x:Name="CalculateBtn"
                Click="SteelTabCalculateButton_Click"/>

            <Button Content="Create Order"
                    x:Name="SaveBtn"
                Style="{StaticResource SecondaryButton}"
                Click="SteelTabSaveButton_Click"/>

        </StackPanel>

    </Grid>
</UserControl>
